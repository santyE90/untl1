# Public Supabase project values. These are safe to expose to the browser when
# Row Level Security and least-privilege grants are configured correctly.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_key
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only. Leave blank until the AI milestone is intentionally enabled.
OPENAI_API_KEY=
