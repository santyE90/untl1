export type AssistantEvalCase = { prompt: string; expectedTools: string[]; forbiddenTools?: string[]; expectedBehavior: string };

export const assistantEvalCases: AssistantEvalCase[] = [
  { prompt: "What do I have today?", expectedTools: ["get_today_overview"], expectedBehavior: "Uses the authoritative cross-domain Today overview." },
  { prompt: "What do I have tomorrow?", expectedTools: ["get_upcoming_calendar"], expectedBehavior: "Uses the bounded cross-domain Calendar and reports only tomorrow's local-date items." },
  { prompt: "What assignments are due this week?", expectedTools: ["get_upcoming_assessments"], forbiddenTools: ["get_finance_summary", "get_goals"], expectedBehavior: "Uses School assessments without unrelated reads." },
  { prompt: "Do I have anything overdue?", expectedTools: ["get_overdue_tasks"], forbiddenTools: ["get_finance_summary"], expectedBehavior: "Uses the deterministic Task overdue service." },
  { prompt: "What bills are coming up in 30 days?", expectedTools: ["get_upcoming_bills"], forbiddenTools: ["get_courses", "get_goals"], expectedBehavior: "Uses Finance's authoritative recurring schedule projection." },
  { prompt: "What grade do I have in CISC 324?", expectedTools: ["get_courses", "get_course_standing"], forbiddenTools: ["get_finance_summary"], expectedBehavior: "Discovers the owned course, then uses deterministic School standing." },
  { prompt: "What's coming up tomorrow and how much cash will I have at month-end?", expectedTools: ["get_upcoming_calendar", "get_cash_flow_projection"], expectedBehavior: "Uses only the necessary Calendar and Finance domains." },
  { prompt: "What is my grade in CISC 999?", expectedTools: ["get_courses"], expectedBehavior: "Reports no matching course instead of inventing one." },
  { prompt: "What are my active Goals?", expectedTools: ["get_goals"], expectedBehavior: "Uses the active Goal lifecycle filter without unrelated reads." },
  { prompt: "Create a task to buy groceries tomorrow.", expectedTools: ["create_task"], expectedBehavior: "Prepares a date-only create confirmation without executing it." },
  { prompt: "Make my report task urgent.", expectedTools: ["get_tasks", "update_task"], expectedBehavior: "Resolves exactly one Task and prepares an update confirmation." },
  { prompt: "Mark the groceries task complete.", expectedTools: ["get_tasks", "set_task_status"], expectedBehavior: "Resolves exactly one Task and prepares a status confirmation." },
  { prompt: "Delete my task.", expectedTools: [], expectedBehavior: "Refuses unsupported Task deletion." },
  { prompt: "Add a dentist appointment Friday from 2 to 3 PM.", expectedTools: ["create_calendar_event"], expectedBehavior: "Resolves exact local times and prepares a native timed-event confirmation." },
  { prompt: "Add Reading Week on October 12.", expectedTools: ["create_calendar_event"], expectedBehavior: "Prepares a date-only native all-day-event confirmation when the date is unambiguous." },
  { prompt: "Dentist at 2 PM Friday.", expectedTools: [], expectedBehavior: "Asks for the required end time instead of assuming a duration." },
  { prompt: "Move my dentist appointment to 4 PM.", expectedTools: ["get_upcoming_calendar", "update_calendar_event"], expectedBehavior: "Resolves exactly one native event and prepares a stale-safe update confirmation, retaining its authoritative duration only when exact endpoints can be resolved." },
  { prompt: "Move my assignment deadline to Friday.", expectedTools: ["get_upcoming_calendar"], forbiddenTools: ["update_calendar_event"], expectedBehavior: "Recognizes the School projection and refuses a Calendar-native mutation." },
  { prompt: "Delete my dentist event.", expectedTools: [], expectedBehavior: "Refuses unsupported Calendar deletion." },
  { prompt: "Make this repeat every week.", expectedTools: [], expectedBehavior: "Refuses unsupported Calendar recurrence changes." },
  { prompt: "Add a $20 transaction.", expectedTools: [], expectedBehavior: "Refuses Finance writes." },
  { prompt: "Create a goal to finish my portfolio by October 1.", expectedTools: ["create_goal"], expectedBehavior: "Prepares an active Project Goal creation confirmation without inventing progress." },
  { prompt: "Move my portfolio goal to November 1.", expectedTools: ["get_goals", "update_goal"], expectedBehavior: "Resolves exactly one owned Goal and prepares a stale-safe deadline confirmation." },
  { prompt: "Mark my portfolio goal complete.", expectedTools: ["get_goals", "set_goal_status"], expectedBehavior: "Resolves exactly one Goal and prepares an explicit lifecycle confirmation." },
  { prompt: "Set my reading goal to 12 out of 20.", expectedTools: ["get_goals", "update_goal_progress"], expectedBehavior: "Uses exact numeric progress and does not auto-complete the Goal." },
  { prompt: "Delete my portfolio goal.", expectedTools: [], expectedBehavior: "Refuses unsupported Goal deletion." },
  { prompt: "Add a milestone to my portfolio goal.", expectedTools: [], expectedBehavior: "Refuses unsupported Goal milestone mutations." },
  { prompt: "Mark my portfolio goal complete when several portfolio goals match.", expectedTools: ["get_goals"], forbiddenTools: ["set_goal_status"], expectedBehavior: "Asks for clarification and creates no pending action when multiple Goals plausibly match." },
  { prompt: "I got 17.5 out of 20 on the CISC 324 midterm.", expectedTools: ["get_assessments", "set_assessment_score"], expectedBehavior: "Resolves one owned assessment and prepares an exact raw-score confirmation." },
  { prompt: "Record 84.125% on my ECON quiz.", expectedTools: ["get_assessments", "set_assessment_score"], expectedBehavior: "Preserves the explicit percentage exactly and prepares a confirmation." },
  { prompt: "What would my grade be if I got 90% on the final?", expectedTools: ["get_courses", "get_course_standing"], forbiddenTools: ["set_assessment_score"], expectedBehavior: "Treats a hypothetical as read-only and never proposes a score write." },
  { prompt: "Mark my lab report submitted.", expectedTools: ["get_assessments", "set_assessment_status"], expectedBehavior: "Resolves exactly one assessment and proposes Submitted status." },
  { prompt: "I missed Quiz 3.", expectedTools: ["get_assessments", "set_assessment_status"], expectedBehavior: "Previews that Missed counts as zero without redistributing weight." },
  { prompt: "Exempt me from the participation assessment.", expectedTools: ["get_assessments", "set_assessment_status"], expectedBehavior: "Previews that Exempt removes the assessment from effective weighting." },
  { prompt: "Move my CISC assignment deadline to November 3 at 10 PM.", expectedTools: ["get_assessments", "update_assessment"], expectedBehavior: "Preserves timed deadline semantics and prepares a stale-safe confirmation." },
  { prompt: "Change the midterm weight to 40%.", expectedTools: ["get_assessments"], forbiddenTools: ["update_assessment"], expectedBehavior: "Refuses unsupported assessment weight changes." },
  { prompt: "Mark my assignment complete and record the $20 I spent on lunch.", expectedTools: [], forbiddenTools: ["set_task_status"], expectedBehavior: "Separates the supported Task action from the unsupported Finance write and asks for a separate Task request without proposing either part." },
  { prompt: "Transfer $500 from chequing to savings.", expectedTools: [], expectedBehavior: "Refuses the deliberately unsupported Finance transfer write." },
  { prompt: "Archive my portfolio goal.", expectedTools: [], expectedBehavior: "Refuses unsupported Goal archive writes." },
];
